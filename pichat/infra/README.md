# Infrastructure

This directory is reserved for future infrastructure-as-code assets (e.g., Helm charts, Terraform) should PiChat host coordination services. Currently, the application is fully peer-to-peer and relies on decentralized Waku nodes, so no centralized infrastructure is required.
